import React from "react";

const Props = ({ name, age }) => {
  return (
    <div>
      <span>
        <p>
          {name}--{age}
        </p>
      </span>
    </div>
  );
};

export default Props;
